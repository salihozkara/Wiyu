version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=Blockchain_Identity
      - Blockchain_DB=Blockchain_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
